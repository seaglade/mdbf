version: 37
jobs:
- name: Build
  steps:
  - !CheckoutStep
    name: Checkout
    cloneCredential: !DefaultCredential {}
    withLfs: false
    withSubmodules: false
    condition: ALL_PREVIOUS_STEPS_WERE_SUCCESSFUL
  - !CommandStep
    name: Detect Build Version
    runInContainer: true
    image: alpine
    interpreter: !DefaultInterpreter
      commands: |
        echo "Detecting buildVersion..."
        sed -n 's/^version = \"\(.*\)\+\"/\1/p' pyproject.toml > build_version
        echo Detected version: $(cat build_version)
    useTTY: true
    condition: ALL_PREVIOUS_STEPS_WERE_SUCCESSFUL
  - !SetBuildVersionStep
    name: Set Build Version
    buildVersion: '@file:build_version@'
    condition: ALL_PREVIOUS_STEPS_WERE_SUCCESSFUL
  - !GenerateChecksumStep
    name: Check Dep Hash
    files: uv.lock pyproject.toml
    targetFile: dep_hash
    condition: ALL_PREVIOUS_STEPS_WERE_SUCCESSFUL
  - !SetupCacheStep
    name: Cache Deps and UV
    key: '@file:dep_hash@'
    paths:
    - .venv
    - .uvcache
    uploadStrategy: UPLOAD_IF_NOT_HIT
    condition: ALL_PREVIOUS_STEPS_WERE_SUCCESSFUL
  - !CommandStep
    name: Install Deps with UV
    runInContainer: true
    image: ghcr.io/astral-sh/uv:python3.12-alpine
    interpreter: !DefaultInterpreter
      commands: |
        uv sync --locked --no-install-project --no-dev --no-editable --cache-dir .uvcache
    useTTY: true
    condition: ALL_PREVIOUS_STEPS_WERE_SUCCESSFUL
  - !CommandStep
    name: Build Library
    runInContainer: true
    image: ghcr.io/astral-sh/uv:python3.12-alpine
    interpreter: !DefaultInterpreter
      commands: |
        uv build --cache-dir .uvcache
    useTTY: true
    condition: ALL_PREVIOUS_STEPS_WERE_SUCCESSFUL
  - !PublishArtifactStep
    name: Publish Artifacts
    artifacts: dist/*.whl dist/*.tar.gz
    condition: ALL_PREVIOUS_STEPS_WERE_SUCCESSFUL
  triggers:
  - !BranchUpdateTrigger
    projects: mdbf
  retryCondition: never
  maxRetries: 3
  retryDelay: 30
  timeout: 14400
- name: Publish
  steps:
  - !CheckoutStep
    name: Checkout
    cloneCredential: !DefaultCredential {}
    withLfs: false
    withSubmodules: false
    condition: ALL_PREVIOUS_STEPS_WERE_SUCCESSFUL
  - !CommandStep
    name: Detect Build Version
    runInContainer: true
    image: alpine
    interpreter: !DefaultInterpreter
      commands: |
        echo "Detecting buildVersion..."
        sed -n 's/^version = \"\(.*\)\+\"/\1/p' pyproject.toml > build_version
        echo Detected version: $(cat build_version)
    useTTY: true
    condition: ALL_PREVIOUS_STEPS_WERE_SUCCESSFUL
  - !SetBuildVersionStep
    name: Set Build Version
    buildVersion: '@file:build_version@'
    condition: ALL_PREVIOUS_STEPS_WERE_SUCCESSFUL
  - !GenerateChecksumStep
    name: Check Dep Hash
    files: uv.lock pyproject.toml
    targetFile: dep_hash
    condition: ALL_PREVIOUS_STEPS_WERE_SUCCESSFUL
  - !SetupCacheStep
    name: Cache Deps and UV
    key: '@file:dep_hash@'
    paths:
    - .venv
    - .uvcache
    uploadStrategy: UPLOAD_IF_NOT_HIT
    condition: ALL_PREVIOUS_STEPS_WERE_SUCCESSFUL
  - !CommandStep
    name: Publish to Registry
    runInContainer: true
    image: python:3.12-alpine
    interpreter: !DefaultInterpreter
      commands: |
        uv publish --cache-dir .uvcache --publish-url https://onedev.kiarc.win/mdbf/~pypi/upload --check-url https://onedev.kiarc.win/mdbf/~pypi/simple -u @job_token@ -p @secret:access-token@
    useTTY: true
    condition: ALL_PREVIOUS_STEPS_WERE_SUCCESSFUL
  jobDependencies:
  - jobName: Build
    requireSuccessful: true
    artifacts: '**'
  retryCondition: never
  maxRetries: 3
  retryDelay: 30
  timeout: 14400
