# Basic MDBF Bot

This directory contains a basic MDBF bot setup. If you add your user ID to `admins` in `config.toml` and the `BOT_TOKEN` environment variable either in your environment or in `compose.yml`, you can run the bot with `uv run main.py` or `docker compose up -d` respectively.
